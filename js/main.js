(function ($) {

    /*
        Parallax effect
        - required
            - element start
            - element end
            - scroll position start
            - scroll position end
            - interpolation/easing function

     */

    var home_element       = null,
        home_begin         = -1,
        home_end           = -1,
        target_element     = null,
        target_begin       = -1,
        target_end         = -1,
        home_parallax      = function (element, target) {

        };

    $(document).ready(function () {
        home_element = $()
    });
}(window.jQuery));